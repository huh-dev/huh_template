

//translations used for the ui - just need to be the name as the locales folders files
export interface Translations {
  ui_heading: string;
  ui_back: string;
}

export type LocaleString = keyof Translations; 